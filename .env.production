# Convex Configuration for Production

# Production Convex deployment URL - use your production deployment URL here
# You'll get this after running `npx convex deploy` and selecting production deployment
CONVEX_URL=https://grateful-ant-828.convex.cloud

# Public Convex URL for client-side usage (same as CONVEX_URL)
NEXT_PUBLIC_CONVEX_URL=https://grateful-ant-828.convex.cloud

# HTTP Actions URL for Convex functions - available as process.env.CONVEX_SITE_URL in Convex functions
# This is typically the same URL with a different subdomain: replace 'grateful-ant-828' with 'grateful-ant-828-site'
CONVEX_SITE_URL=https://grateful-ant-828-site.convex.site

# Environment flag
NODE_ENV=production

# Domain configuration for CORS (production)
# Replace with your actual production domain
NEXT_PUBLIC_DOMAIN=https://ngc.oblak.solutions

# Optional: Email service configuration (for notifications)
# RESEND_API_KEY=your_resend_api_key_here
# SENDGRID_API_KEY=your_sendgrid_api_key_here

# If you're using server actions or API routes that need to call Convex HTTP endpoints
NEXT_PUBLIC_CONVEX_HTTP_URL=https://grateful-ant-828-site.convex.cloud

# Optional: Analytics and monitoring
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id
# SENTRY_DSN=your_sentry_dsn
# Main API URL: https://grateful-ant-828.convex.cloud
# HTTP Actions URL: https://grateful-ant-828-site.convex.cloud
